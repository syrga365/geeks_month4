import React from 'react';
import Title from "../title/Title";

const Footer = () => {
    return (
        <div>
            <Title title={'Footer'}/>
        </div>
    );
};

export default Footer;