import React from 'react';
import Title from "../title/Title";

const Header = () => {
    return (
        <div>
            <Title title={'Header'}/>
        </div>
    );
};

export default Header;